<!DOCTYPE html>
<html lang="en">
    <%- include("../partials/head") %>
<body>
    <%- include("../partials/header") %>
    <div class="registro">
        <h2>Hola <%= user.nombre %></h2>
        <form class="form-container" action="/users/update/<%= users.id %>?_method=PUT" method="POST">
            <p class="titulo-login">EDITÁ TU CUENTA:</p>
            <label for="nombre">Nombre:</label> <br>
            <input type="text" name="nombre" id="nombre" class="formulario-input <%= locals.errors && errors.nombre ? 'is-invalid' : null %>" value="<%=users.nombre%>" required> <br>
            <label for="apellido">Apellido:</label> <br>
            <input type="text" name="apellido"  class="formulario-input <%= locals.errors && errors.apellido ? 'is-invalid' : null %>" value="<%=users.apellido%>" required> <br>
        
            <label for="email">Email:</label> <br>
            <input type="email" name="email" class="formulario-input <%= locals.errors && errors.email ? 'is-invalid' : null %>"  value="<%=users.email%>" required disabled>
           <br>
            <label for="password">Contraseña:</label><br>
            <input type="password" name="password" class="formulario-input <%= locals.errors && errors.password ? 'is-invalid' : null %>" value="<%=users.password%>" disabled> <br>
            <label for="password">Repita la contraseña:</label><br>
            <input type="password" name="confirmPassword"  class="formulario-input <%= locals.errors && errors.confirmPassword ? 'is-invalid' : null %>" value="<%=users.confirmPassword%>" disabled> <br>
            <div class="botones">
            <button class="entrar" type="submit">ACTUALIZAR</button>
            </div>
        </form>
        <form onSubmit="return confirm('Estas seguro de querer borrar el usuario?')" action="/users/delete/<%= users.id %>?_method=DELETE" method="POST">
            <button class="entrar" type="submit">ELIMINAR</button>
        </form>
    </div>
    <%- include("../partials/footer") %>     
    </body>
</html>